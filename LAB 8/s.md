| Ex.No | Task                              | Technique Used         | Function / Keyword                                                                           | 1-Line Explanation                                                |
| ----- | --------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 1     | Line plot of total profit         | Line Visualization     | `pd.read_csv()` + `plt.plot()`                                                               | Reads CSV and plots total profit vs month as a line graph.        |
| 2     | Styled line plot                  | Plot Styling           | `linestyle='--'`, `color='red'`, `marker='o'`, `linewidth=`, `plt.legend(loc='lower right')` | Customizes line style, color, marker, legend position, and width. |
| 2     | Axis labeling                     | Axis labeling          | `plt.xlabel()` + `plt.ylabel()`                                                              | Sets descriptive labels for X and Y axes.                         |
| 3     | Multiline plot (all products)     | Multiple Line Plot     | `plt.plot()` multiple times                                                                  | Plots separate lines for each product on same graph.              |
| 4     | Scatter plot (toothpaste sales)   | Scatter Visualization  | `plt.scatter()`                                                                              | Displays monthly toothpaste sales as data points.                 |
| 5     | Bar chart (face cream & facewash) | Categorical Comparison | `plt.bar()`                                                                                  | Creates bar chart comparing two product sales monthly.            |
| 6     | Bar chart (bathing soap) + Save   | File Output            | `plt.bar()` + `plt.savefig()`                                                                | Plots bar chart and saves image file to disk.                     |
| 7     | Histogram (total profit)          | Distribution Analysis  | `plt.hist()`                                                                                 | Shows frequency distribution of profit ranges.                    |
| 8     | Pie chart (yearly product sales)  | Aggregation + Pie Plot | `sum()` + `plt.pie()`                                                                        | Calculates yearly totals and visualizes percentage share.         |
| 9     | Subplot (soap & facewash)         | Multiple Axes Layout   | `plt.subplot()`                                                                              | Displays multiple related plots in one figure.                    |
| 10    | Stack plot (all products)         | Area Visualization     | `plt.stackplot()`                                                                            | Displays cumulative product sales as stacked area chart.          |
