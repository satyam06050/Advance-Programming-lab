Python Lab Assignment-5
[Python OOP Concept]

Q.1) Write a python program to create a class - box with three data members(length, width,
height) and a method volume(). Then create an object of the box class and print the volume
of that object.
Q.2) Write a python code to create a company’s employee hierarchy using different classes.
Create following classes - 

. Employee class - An employee class should have object attributes to store
basic info about an employee like name (public), age(private),
experience(public), department(public), salary(private) and also should have
a class attribute called companyName which should be initialized as the
name of the company. Employee class should also have few methods
defining functionalities of an employee.
. Manager class - Manager class should inherit the properties of Employee
class and also have some properties like a list of teams supervised by a
manager. Also, methods like getting the number of total teams managed by a
manager.
a. Developer class - class to represent a software developer. Add some
attributes and methods which you think are specific to a developer.
Q.3) Write a python code a class that represents a binary tree, which is a tree data structure
in which each node has at most two children, which are referred to as the left child and the
right child. Each tree node is a class that contains a value.
The class for a binary tree provides methods for - 
 Computing the height of the tree
 Counting the number of nodes
 Adding new nodes by automatically checking where it is possible to add a
new node in the binary tree
 Removing nodes